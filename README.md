# QA-Automation-Repo

This automation code caters to BDD using Playwright Java and Cucumber

Properties can be configured under src/test/resources/config.properties file.

## Command to run framework from command line
`mvn clean test`
